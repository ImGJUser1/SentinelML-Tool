# SentinelML Architecture

SentinelML provides a runtime trust layer for machine learning systems.

Pipeline:

Model → Predictions → Sentinel Monitor → Trust Metrics → Alerts

Components:

1. Drift Detector
2. Uncertainty Estimator
3. Adaptive Trust Manifold (ATM)
4. Runtime Alert System

```
